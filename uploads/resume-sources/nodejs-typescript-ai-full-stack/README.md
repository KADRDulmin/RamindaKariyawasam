# Node.js / TypeScript + AI Full Stack resume

Edit `content.json`, then regenerate from the portfolio root:

```powershell
python "uploads/resume-sources/build_resumes.py" nodejs-typescript-ai-full-stack
```
